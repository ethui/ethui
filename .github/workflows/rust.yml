name: rust

on:
  push:
    branches:
      - main
    tags:
      - "*"
  pull_request:
    types: [opened, synchronize]
    paths-ignore:
      - "./extension/**"
      - "./gui/**"
      - "*.json"
      - "*.md"
      - "LICENSE"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  download:
    runs-on: windows-latest
    steps:
      - run: |
          C:\mysys64\usr\bin\curl.exe https://download.libsodium.org/libsodium/releases/libsodium-1.0.18-msvc.zip --output libsodium.zip
          7z x libsodium.zip
          Copy-Item -Path libsodium\x64\Release\v142\static\libsodium.lib sodium.lib
