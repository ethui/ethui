procs:
  app:
    cmd: ["yarn", "run", "app:dev"]
  ext:
    cmd: ["yarn", "run", "ext:watch"]
  storybook:
    cmd: ["yarn", "run", "storybook"]
  anvil:
    cmd: ["anvil", "--host", "0.0.0.0"]
  demo:
    cmd: ["yarn", "demo:dev"]
  demo-contracts:
    cmd: ["yarn", "demo:eth-watch"]
