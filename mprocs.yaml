procs:
  app:
    cmd: ["yarn", "run", "app:dev"]
  chrome-ext:
    cmd: ["yarn", "run", "ext:watch:chrome"]
  firefox-ext:
    cmd: ["yarn", "run", "ext:watch:firefox"]
  storybook:
    cmd: ["yarn", "run", "storybook"]

env:
  CARGO_TERM_COLOR: always
