procs:
  app:
    cmd: ["pnpm", "dev"]
  anvil:
    cmd: ["anvil", "--host", "0.0.0.0"]
  demo/web:
    cwd: "../demo"
    cmd: ["yarn", "dev"]
  demo/contracts:
    cwd: "../demo"
    cmd: ["yarn", "eth-watch"]
  ui/storybook:
    cwd: "../ui"
    cmd: ["yarn", "dev"]
  explorer:
    cwd: "../explorer"
    cmd: ["yarn", "dev"]

